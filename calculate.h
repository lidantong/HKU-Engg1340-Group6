#ifndef CALCULATE_H

#define CALCULATE_H

pair <int, double> get_max(int max_counter, double counter_cost, double time_limit, int customer_cnt,  vector<customer>);

double process(double time_limit, int counter_cnt, int customer_cnt, vector<customer>);

#endif