#ifndef CALCULATE_H

#define CALCULATE_H

double get_max(int max_counter, double counter_cost, double time_limit, int customer_cnt, customer *c);

double process(double time_limit, int counter_cnt, int customer_cnt, customer *c);

#endif